add_newline = false
command_timeout = 60
format = """$os $directory $git_branch$git_status$line_break$character"""

# right_format = """# $time# """

palette = "colors"

[palettes.colors]
mustard = '#af8700'
color1 = '#8fbcbb'        # directory path (primary accent)
color2 = '#2e3440'          # unused
color3 = '#d8dee9'          # unused
color4 = '#2e3440'          # unused
color5 = '#2e3440'          # unused
color6 = '#4c566a'        # vicmd symbol (muted gray)
color7 = '#2e3440'          # unused
color8 = '#8fbcbb'        # os symbol, success character (primary)
color9 = '#b48ead'     # git branch (secondary accent)
error = '#bf616a'          # error character

[directory]
style = "fg:color1 bold"
read_only = " 󰌾"
read_only_style = "fg:#bf616a"
truncation_length = 0
truncate_to_repo = false
format = "[$path]($style)[$read_only]($read_only_style)"

[directory.substitutions]
'~/100xCodes/Cohort-3-WebDev' = 'Learning'
'~/Testing-repo' = 'work-project'

[character]
success_symbol = '[❯](fg:color8 bold)'
error_symbol = '[❯](fg:error bold)'
vicmd_symbol = '[❮](fg:color6 bold)'

[git_branch]
style = "fg:color9 bold"
format = '[$symbol$branch]($style) '

[git_status]
ahead = '⇡${count}'
behind = '⇣${count}'
diverged = '⇕⇡${ahead_count}⇣${behind_count}'
format = '[[($all_status$ahead_behind )](fg:#bf616a)]($style)'

[os]
format = "[$symbol]($style)"
style = "fg:color8 bold"
disabled = false

[os.symbols]
Linux = "󰊠 "
Arch = "󰣇"
Ubuntu = "󰕈"

# [time]
# disabled = false
# format = '[[ $time ](fg:#bf616a )]($style)'
# time_format = "%R"

[line_break]
disabled = true

