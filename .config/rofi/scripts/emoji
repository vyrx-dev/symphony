#!/bin/bash

# Kill existing rofi
pidof rofi >/dev/null && pkill rofi

rofimoji \
	--skin-tone neutral \
	--hidden-descriptions \
	--clipboarder wl-copy \
	--typer wtype \
	--selector-args="-theme ~/.config/rofi/custom-rofi/config-emoji.rasi"
# --action clipboard \ show in clipboard history
